[project]
name = "top-and-tail"
version = "0"
requires-python = ">=3.13"

[dependency-groups]
dev = ["pytest>=8", "mutagen>=1.47"]

[tool.uv]
package = false

[tool.pytest.ini_options]
testpaths = ["tests"]
